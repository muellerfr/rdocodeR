% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdoc_setup.R
\name{rdoc_setup}
\alias{rdoc_setup}
\title{Set Up Cached RDoC Term Null Maps}
\usage{
rdoc_setup(
  n_nulls = rdoc_default_n_nulls(),
  force = FALSE,
  python = NULL,
  terms = NULL,
  n_jobs = 1L,
  quiet = FALSE
)
}
\arguments{
\item{n_nulls}{Number of eigenstraps to generate per term. Default is \code{1000}.}

\item{force}{Logical; if \code{TRUE}, regenerate files even when they already exist.}

\item{python}{Optional Python interpreter path. If \code{NULL}, \code{rdocodeR} creates and reuses
a private virtual environment in the user cache directory.}

\item{terms}{Optional character vector of term ids to generate. Defaults to all bundled terms.}

\item{n_jobs}{Number of Python jobs used per hemisphere during eigenstrap generation.}

\item{quiet}{Logical; suppress progress messages.}
}
\value{
Invisibly returns a list with cache metadata and generated files.
}
\description{
Creates or refreshes the cached per-term eigenstrap null maps used by \code{\link[=rdoc_decode]{rdoc_decode()}}.
These null maps are generated locally for the user and are not shipped precomputed with the
package. By default, the setup generates \code{1000} nulls per bundled RDoC term map and stores
them in the user cache directory returned by \code{\link[=rdoc_term_nulls_dir]{rdoc_term_nulls_dir()}}.
}
